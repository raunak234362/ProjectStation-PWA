const SectionTitle = ({ title }: { title: string }) => (
  <h2 className="text-base md:text-lg font-black text-black border-b border-gray-100 pb-1 uppercase tracking-tight mb-4">
    {title}
  </h2>
);
export default SectionTitle;
