const QuotationRaise = ({rfqId, onClose, onSuccess}: {rfqId: string, onClose: () => void, onSuccess: () => void}) => {
    return (
        <div>
            <h1>Quotation Raise</h1>
        </div>
    );
};

export default QuotationRaise;
